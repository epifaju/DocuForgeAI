# TLS certificates for Traefik TLS_MODE=file (on-prem / LAN).
# Place fullchain.pem and privkey.pem here (gitignored).
# Generate a local self-signed pair: scripts/gen-dev-certs.ps1 or scripts/gen-dev-certs.sh
